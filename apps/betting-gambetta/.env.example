# En dev, l’API est appelée via le proxy Vite (pas besoin de VITE_API_URL).

# URL du front Betting (pour les liens depuis le site Poker). En dev : http://localhost:5174
# VITE_BETTING_APP_URL=http://localhost:5174

# URL du front Poker (pour l’onglet « Poker » dans la nav Betting). En dev : http://localhost:5173
# VITE_POKER_APP_URL=http://localhost:5173
